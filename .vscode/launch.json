{
	"version": "0.2.0",
	"configurations": [
		{
			"envFile": "${workspaceFolder}/.env",
			"command": "npm run dev",
			"name": "Run development server",
			"request": "launch",
			"type": "node-terminal"
		}
	]
}